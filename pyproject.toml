[project]
name = "stock-analyzer-hsg"
version = "0.0.0"
requires-python = ">=3.13,<3.14"

# Dependencies are managed in requirements.txt for runtime installs.
# This file primarily enforces the Python version for installers/tools.
